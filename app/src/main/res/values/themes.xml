<?xml version="1.0" encoding="utf-8"?>
<resources>
    <!-- Base application theme (Dark/Material 3) -->
    <style name="Theme.CommunityAlert" parent="Theme.Material3.Dark.NoActionBar">
        <item name="colorPrimary">@color/primary</item>
        <item name="colorPrimaryVariant">@color/primary_variant</item>
        <item name="colorSecondary">@color/secondary</item>
        <item name="colorSecondaryVariant">@color/secondary_variant</item>
        <item name="colorError">@color/error</item>
        <item name="colorOnPrimary">@color/on_primary</item>
        <item name="colorOnSecondary">@color/on_secondary</item>
        <item name="colorOnError">@color/on_error</item>
        <item name="android:colorBackground">@color/background_dark</item>
        <item name="colorSurface">@color/surface_dark</item>
        <item name="colorOnSurface">@color/on_surface_dark</item>
        <item name="colorOnBackground">@color/on_background_dark</item>
        <item name="colorSurfaceVariant">@color/surface_variant_dark</item>
        <item name="android:statusBarColor">@color/background_dark</item>
        <item name="android:navigationBarColor">@color/background_dark</item>
    </style>

    <style name="Theme.CommunityAlert.NoActionBar">
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
    </style>

    <style name="Theme.CommunityAlert.AppBarOverlay" parent="ThemeOverlay.Material3.Dark.ActionBar" />

    <style name="Theme.CommunityAlert.PopupOverlay" parent="ThemeOverlay.Material3.Dark" />

    <!-- Card Style -->
    <style name="AlertCardStyle" parent="Widget.Material3.CardView.Elevated">
        <item name="cardBackgroundColor">@color/card_background</item>
        <item name="cardCornerRadius">12dp</item>
        <item name="cardElevation">2dp</item>
        <item name="strokeColor">@color/card_stroke</item>
        <item name="strokeWidth">1dp</item>
    </style>

    <!-- Severity chip styles -->
    <style name="SeverityChipInfo" parent="Widget.Material3.Chip.Suggestion">
        <item name="chipBackgroundColor">@color/severity_info</item>
        <item name="android:textColor">@color/on_primary</item>
    </style>

    <style name="SeverityChipWarning" parent="Widget.Material3.Chip.Suggestion">
        <item name="chipBackgroundColor">@color/severity_warning</item>
        <item name="android:textColor">@color/on_primary</item>
    </style>

    <style name="SeverityChipCritical" parent="Widget.Material3.Chip.Suggestion">
        <item name="chipBackgroundColor">@color/severity_critical</item>
        <item name="android:textColor">@color/on_primary</item>
    </style>
</resources>